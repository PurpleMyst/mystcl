---

language: minimal

services:
  - docker

before_install:
  - docker build -t mystcl .

script:
  - docker run mystcl ./test

deploy:
  provider: script
  script: docker run mystcl ./deploy
  on:
    tags: true
