---

language: python

services:
  - docker

before_install:
  - docker build -t mystcl .

script:
  - docker run -v $TRAVIS_BUILD_DIR:/app mystcl

deploy:
  provider: script
  script: docker run -e TWINE_USERNAME -e TWINE_PASSWORD mystcl ./deploy
  skip_cleanup: true
  on:
    tags: true
